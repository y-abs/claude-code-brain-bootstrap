# Build, Test & CI Reference — {{PROJECT_NAME}}

<!-- BOOTSTRAP: This file is auto-generated by the bootstrap prompt. Fill manually or run /bootstrap. -->

## Package Manager & Runtime

- **Package manager**: {{PACKAGE_MANAGER}} (version: {{PACKAGE_MANAGER_VERSION}})
- **Runtime**: {{RUNTIME}} (version: {{RUNTIME_VERSION}})
<!-- Examples: pnpm 10+, npm 10+, pip 24+, cargo 1.75+, go 1.22+, maven 3.9+ -->

## Build Commands

| Scope | Command |
|-------|---------|
| All | `{{BUILD_CMD_ALL}}` |
| Single service/module | `{{BUILD_CMD_SINGLE}}` |
| Packages/libraries only | `{{BUILD_CMD_PACKAGES}}` |
<!-- Examples: pnpm build, cargo build --release, mvn clean install -DskipTests -->

## Test Commands

| Scope | Command |
|-------|---------|
| All tests | `{{TEST_CMD_ALL}}` |
| Single service | `{{TEST_CMD_SINGLE}}` |
| CI mode | `{{TEST_CMD_CI}}` |
| With coverage | `{{TEST_CMD_COVERAGE}}` |
<!-- Examples: pnpm test, pytest, cargo test, mvn test -->

## Lint & Format Commands

| Action | Command |
|--------|---------|
| Check (report only) | `{{LINT_CHECK_CMD}}` |
| Fix (auto-fix) | `{{LINT_FIX_CMD}}` |
| Format only | `{{FORMAT_CMD}}` |
<!-- Examples: biome check, eslint ., ruff check, cargo clippy -->

## CI Pipeline

<!-- Describe your CI stages -->
<!-- Example:
1. Install dependencies
2. Lint/format check
3. Build
4. Unit tests
5. Integration tests
6. Security scan
7. Docker build
-->

## Local Development

### Prerequisites
<!-- List required tools: runtime, package manager, database, message broker, etc. -->

### Quick Start
```bash
# {{INSTALL_CMD}}
# {{DEV_CMD}}
```

### ⚠️ Pitfalls (from lessons)
<!-- Add known issues discovered during development -->
<!-- Example: Service X has `process.exit(0)` in test — run tests in isolation -->

