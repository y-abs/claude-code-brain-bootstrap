---
paths:
  - "{{SERVICE_DIR}}/**"
  # Add more path patterns that should trigger this rule
---

# {{DOMAIN_NAME}} — Critical Patterns

> Full reference: `claude/{{DOMAIN_NAME}}.md`
> Copy this file, rename to `<domain>.md`, fill paths and patterns.

## Key patterns for this domain

<!-- Add 5-10 critical NEVER/ALWAYS rules specific to this domain -->
<!-- Keep to ≤40 lines total — reference the full doc for details -->

<!-- Example patterns:
- ⚠️ Never write to the read-only database from service layer
- ⚠️ Always validate input at the controller layer, not the service layer
- ⚠️ Status transitions must go through the state machine — never set status directly
-->

